# Exercise Data Log

This folder contains a Streamlit conversion of `exercise_dashboard1.jsx`.

Run locally:

```powershell
cd C:\Users\LENOVO\Documents\Codex\2026-07-01\files-mentioned-by-the-user-r\outputs
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run streamlit_app.py
```

The app loads its dashboard data from `exercise_dashboard_data.json`, which was extracted from the JSX source and bundled with the Streamlit version.
